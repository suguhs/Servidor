*Dada la siguiente línea de código $valor = " Es tu nombre O\'reilly? ";

1-Evalúa que resultado produce la siguiente línea de código e indica qué valor tiene la variable $resultado.
$resultado = trim($valor);

2-Evalúa que resultado produce la siguiente línea de código e indica qué valor tiene la variable $resultado.

$resultado = stripslashes($valor);

1- En el primer caso hace que la funcion trim() hace que elimine los espacios que hay al principio y al final.

$resultado = trim($valor); imprimira -->  $valor = "Es tu nombre O\'reilly?";

2- La funcion stripslashes() hace que se elimine las barras laterales de las variables "\"

$resultado = stripslashes($valor); imprimira --> $valor = " Es tu nombre O'reilly? ";
